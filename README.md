# 2D Open World Template

Open `index.html` in a browser to run the simple 2D open-world template.

Controls:
- Arrow keys or WASD: move camera
- Drag mouse: pan
- Scroll wheel: zoom

No build required — just open the file in your browser (Chrome/Edge/Firefox).
